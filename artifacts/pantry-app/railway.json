{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install -g pnpm && pnpm install --filter @workspace/pantry-app && pnpm --filter @workspace/pantry-app run build"
  },
  "deploy": {
    "startCommand": "npx serve -s dist -l 8082",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
